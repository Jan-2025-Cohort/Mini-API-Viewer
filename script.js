const output = document.getElementById("output");
const asyncBtn = document.getElementById("asyncBtn");
const thenBtn = document.getElementById("thenBtn");

asyncBtn.addEventListener("click", [This is where you add your new function]);

thenBtn.addEventListener("click", [This is where you add your new function]);

function displayImages(images) {
  // Add this function that displays the images on the page.
}
